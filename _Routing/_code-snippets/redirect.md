# Redirect

```js
const Page = () => {
  return <Redirect href="/(auth)/welcome" />
};
```

```js

 <TouchableOpacity
        onPress={() => {
          router.replace("/(auth)/sign-up");
        }}
      >
        <Text className="">Skip</Text>
      </TouchableOpacity>
```
