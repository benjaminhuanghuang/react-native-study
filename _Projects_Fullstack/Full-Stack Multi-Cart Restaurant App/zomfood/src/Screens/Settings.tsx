import React from "react";
import { TabsStackScreenProps } from "../Navigation/TabsNavigation";

function Settings({ navigation, route }: TabsStackScreenProps<"Settings">) {
  return <div>Settings</div>;
}

export default Settings;
